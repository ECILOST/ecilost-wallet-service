generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Las entidades se definirán al implementar HU-09. El servicio mantiene un
// esquema propio (wallet) y nunca comparte la base de datos de auth-service.
